// 1-1. Run the “hello, world" program on your system. Experiment  with leaving out parts of the program, to see what error messages you get.

// Standard Library for I/O
# include <stdio.h>
// Define a function named main that receives no argument values and return an integer
// Compiler will find this out to compile first, for every program
int main(void)
// Statements of main (and every functions) are enclosed in braces
{
// main calls library function printf to print this sequence of characters
// \n represents the newline character
   printf("hello, world\n");
// return 0 by the function
   return 0;
}
